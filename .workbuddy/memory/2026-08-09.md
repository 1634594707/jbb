# 2026-08-09

## JBB Mirror 部署
- 修改 `_worker.js` 推广码：`JUJ7` → `iVAt`（服务端常量 + 注入脚本各一处）。
- 初始化 Git 仓库并推送至 `git@github.com:1634594707/jbb.git`，分支 `main`。
- 在 Cloudflare Workers & Pages 完成部署，Worker 名称 `jbb`，版本 `1eef4ff5` 已上线。
- 部署配置：保持构建/部署命令为 `npx wrangler deploy`，Wrangler 自动生成 `wrangler.jsonc`，`assets.directory: "."`，`_worker.js` 作为 Worker 入口。
